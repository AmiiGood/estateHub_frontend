/**
 * Archivo que genera el contexto, creando un objetivo Context mediante la función CreateContext
 */

import { createContext } from "react";

const UsuarioContext = createContext();

export default UsuarioContext;